using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CombatManager : MonoBehaviour
{
    //public bool CanAttack(Unit attacker, Unit target)
    //{
    //    // 1. Safety check: Make sure neither is null
    //    if (attacker == null || target == null) return false;

    //    // 2. Health check: Can't attack a corpse
    //    if (target.isDead) return false;

    //    // 3. Team check: Only attack if teams are different
    //    if (attacker.unitTeam != target.unitTeam)
    //    {
    //        return true;
    //    }

    //    // If they are on the same team
    //    return false;
    //}
}
